import ClientSide from './ClientSide'

// mini search prototype with autocomplete < 100k items
// translate to prod ready Elastic Search

const Page = () => {
  return (<div>
    <ClientSide />
  </div>)
}

export default Page
